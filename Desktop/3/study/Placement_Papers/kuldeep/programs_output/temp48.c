 #define MAX(a, b) a>b ? a:b
         main()
         {
                int m, n;
               m = 3 + MAX(2, 3);
                n = 2 * MAX(3, 2);
              printf("m = %d, n = %d\n", m, n);
        }

